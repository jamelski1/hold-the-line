# Data files (downloaded at runtime)
data/big_tool_des.json
data/all_clean_data.csv
data/MetaTool/
# data/sampled_data.csv is the assignment deliverable — keep it tracked.

# Results — track the deliverable-relevant ones; only ignore the raw
# malformed-response logs (which can be large and aren't a deliverable).
results/malformed_*.jsonl

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/

# HF cache (if run locally)
.hf_cache/
